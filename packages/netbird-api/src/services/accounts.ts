import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
// AUTO-GENERATED by scripts/generate.ts from .generated-specs (spec/netbird.api.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import { UnknownNetbirdError } from "../errors.ts";
import { NetbirdProtocol, type NetbirdOpError, type NetbirdOpContext } from "../protocol.ts";
import * as Retry from "../retry.ts";
import * as T from "../traits.ts";

export type { NetbirdOpError, NetbirdOpContext };

export class BadRequest
	extends /*@__PURE__*/ T.applyErrorMatchers(
		/*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
			code: S.Number,
			message: S.String,
		}).pipe(C.withBadRequestError),
		[{ status: 400 }],
	) {}

export class Forbidden
	extends /*@__PURE__*/ T.applyErrorMatchers(
		/*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
			code: S.Number,
			message: S.String,
		}).pipe(C.withAuthError),
		[{ status: 403 }],
	) {}

export interface AccountsAccountIdDeleteRequest {
	/** The unique identifier of an account */
	accountId: string;
}
export const AccountsAccountIdDeleteRequest = /*@__PURE__*/ S.suspend(() =>
	S.Struct({
		accountId: S.String.pipe(T.Label()),
	}).pipe(T.Http({ method: "DELETE", uri: "/api/accounts/{accountId}", code: 200 })),
).annotate({ identifier: "AccountsAccountIdDeleteRequest" }) as any as S.Schema<AccountsAccountIdDeleteRequest>;

export interface AccountsAccountIdDeleteResponse {}
export const AccountsAccountIdDeleteResponse = /*@__PURE__*/ S.suspend(() => S.Struct({})).annotate({
	identifier: "AccountsAccountIdDeleteResponse",
}) as any as S.Schema<AccountsAccountIdDeleteResponse>;

/** List of groups to which users are allowed access */
export type AccountSettingsInputJwtAllowGroupsList = ReadonlyArray<string>;
export const AccountSettingsInputJwtAllowGroupsList = /*@__PURE__*/ S.Array(
	S.String,
) as any as S.Schema<AccountSettingsInputJwtAllowGroupsList>;

/** Limits which peer groups are allowed to expose services. If empty, all peers are allowed when peer expose is enabled. */
export type AccountSettingsInputPeerExposeGroupsList = ReadonlyArray<string>;
export const AccountSettingsInputPeerExposeGroupsList = /*@__PURE__*/ S.Array(
	S.String,
) as any as S.Schema<AccountSettingsInputPeerExposeGroupsList>;

/** Limits traffic logging to these groups. If unset all peers are enabled. */
export type AccountExtraSettingsNetworkTrafficLogsGroupsList = ReadonlyArray<string>;
export const AccountExtraSettingsNetworkTrafficLogsGroupsList = /*@__PURE__*/ S.Array(
	S.String,
) as any as S.Schema<AccountExtraSettingsNetworkTrafficLogsGroupsList>;

export interface AccountExtraSettings {
	/** (Cloud only) Enables or disables peer approval globally. If enabled, all peers added will be in pending state until approved by an admin. */
	peer_approval_enabled: boolean;
	/** Enables manual approval for new users joining via domain matching. When enabled, users are blocked with pending approval status until explicitly approved by an admin. */
	user_approval_required: boolean;
	/** Enables or disables network traffic logging. If enabled, all network traffic events from peers will be stored. */
	network_traffic_logs_enabled: boolean;
	/** Limits traffic logging to these groups. If unset all peers are enabled. */
	network_traffic_logs_groups: AccountExtraSettingsNetworkTrafficLogsGroupsList;
	/** Enables or disables network traffic packet counter. If enabled, network packets and their size will be counted and reported. (This can have an slight impact on performance) */
	network_traffic_packet_counter_enabled: boolean;
}
export const AccountExtraSettings = /*@__PURE__*/ S.suspend(() =>
	S.Struct({
		peer_approval_enabled: S.Boolean,
		user_approval_required: S.Boolean,
		network_traffic_logs_enabled: S.Boolean,
		network_traffic_logs_groups: AccountExtraSettingsNetworkTrafficLogsGroupsList,
		network_traffic_packet_counter_enabled: S.Boolean,
	}),
).annotate({ identifier: "AccountExtraSettings" }) as any as S.Schema<AccountExtraSettings>;

/** Per-account dashboard section visibility overrides. Omitted keys follow the default dashboard behavior. */
export interface AccountDashboardFeatures {
	/** Controls the Agent Network menu for the account regardless of the deployment feature flag. When true the menu is shown, when false it is hidden, and when omitted the default behavior applies. Must be true when agent_network_only is enabled. */
	agent_network?: boolean;
}
export const AccountDashboardFeatures = /*@__PURE__*/ S.suspend(() =>
	S.Struct({
		agent_network: S.optional(S.Boolean),
	}),
).annotate({ identifier: "AccountDashboardFeatures" }) as any as S.Schema<AccountDashboardFeatures>;

/** List of group IDs whose peers receive IPv6 overlay addresses. Peers not in any of these groups will not be allocated an IPv6 address. New accounts default to the All group. */
export type AccountSettingsInputIpv6EnabledGroupsList = ReadonlyArray<string>;
export const AccountSettingsInputIpv6EnabledGroupsList = /*@__PURE__*/ S.Array(
	S.String,
) as any as S.Schema<AccountSettingsInputIpv6EnabledGroupsList>;

export interface AccountSettingsInput {
	/** Enables or disables peer login expiration globally. After peer's login has expired the user has to log in (authenticate). Applies only to peers that were added by a user (interactive SSO login). */
	peer_login_expiration_enabled: boolean;
	/** Period of time after which peer login expires (seconds). */
	peer_login_expiration: number;
	/** Enables or disables peer inactivity expiration globally. After peer's session has expired the user has to log in (authenticate). Applies only to peers that were added by a user (interactive SSO login). */
	peer_inactivity_expiration_enabled: boolean;
	/** Period of time of inactivity after which peer session expires (seconds). */
	peer_inactivity_expiration: number;
	/** Allows blocking regular users from viewing parts of the system. */
	regular_users_view_blocked: boolean;
	/** Allows propagate the new user auto groups to peers that belongs to the user */
	groups_propagation_enabled?: boolean;
	/** Allows extract groups from JWT claim and add it to account groups. */
	jwt_groups_enabled?: boolean;
	/** Name of the claim from which we extract groups names to add it to account groups. */
	jwt_groups_claim_name?: string;
	/** List of groups to which users are allowed access */
	jwt_allow_groups?: AccountSettingsInputJwtAllowGroupsList;
	/** Enables or disables DNS resolution on the routing peers */
	routing_peer_dns_resolution_enabled?: boolean;
	/** Allows to define a custom dns domain for the account */
	dns_domain?: string;
	/** Allows to define a custom network range for the account in CIDR format */
	network_range?: string;
	/** Allows to define a custom IPv6 network range for the account in CIDR format. */
	network_range_v6?: string;
	/** Enables or disables peer expose. If enabled, peers can expose local services through the reverse proxy using the CLI. */
	peer_expose_enabled: boolean;
	/** Limits which peer groups are allowed to expose services. If empty, all peers are allowed when peer expose is enabled. */
	peer_expose_groups: AccountSettingsInputPeerExposeGroupsList;
	extra?: AccountExtraSettings;
	/** Enables or disables experimental lazy connection */
	lazy_connection_enabled?: boolean;
	/** Set Clients auto-update version. "latest", "disabled", or a specific version (e.g "0.50.1") */
	auto_update_version?: string;
	/** When true, updates are installed automatically in the background. When false, updates require user interaction from the UI. */
	auto_update_always?: boolean;
	/** Enables or disables client metrics push for all peers in the account */
	metrics_push_enabled?: boolean;
	/** Limits the dashboard to the Agent Network surface for this account. Set for accounts created via netbird.ai signups and can be disabled later. Enabling this requires dashboard_features.agent_network to be true in the same request. */
	agent_network_only?: boolean;
	dashboard_features?: AccountDashboardFeatures;
	/** Enables or disables TOTP multi-factor authentication for local users. Only applicable when the embedded identity provider is enabled. */
	local_mfa_enabled?: boolean;
	/** List of group IDs whose peers receive IPv6 overlay addresses. Peers not in any of these groups will not be allocated an IPv6 address. New accounts default to the All group. */
	ipv6_enabled_groups?: AccountSettingsInputIpv6EnabledGroupsList;
}
export const AccountSettingsInput = /*@__PURE__*/ S.suspend(() =>
	S.Struct({
		peer_login_expiration_enabled: S.Boolean,
		peer_login_expiration: S.Number,
		peer_inactivity_expiration_enabled: S.Boolean,
		peer_inactivity_expiration: S.Number,
		regular_users_view_blocked: S.Boolean,
		groups_propagation_enabled: S.optional(S.Boolean),
		jwt_groups_enabled: S.optional(S.Boolean),
		jwt_groups_claim_name: S.optional(S.String),
		jwt_allow_groups: S.optional(AccountSettingsInputJwtAllowGroupsList),
		routing_peer_dns_resolution_enabled: S.optional(S.Boolean),
		dns_domain: S.optional(S.String),
		network_range: S.optional(S.String),
		network_range_v6: S.optional(S.String),
		peer_expose_enabled: S.Boolean,
		peer_expose_groups: AccountSettingsInputPeerExposeGroupsList,
		extra: S.optional(AccountExtraSettings),
		lazy_connection_enabled: S.optional(S.Boolean),
		auto_update_version: S.optional(S.String),
		auto_update_always: S.optional(S.Boolean),
		metrics_push_enabled: S.optional(S.Boolean),
		agent_network_only: S.optional(S.Boolean),
		dashboard_features: S.optional(AccountDashboardFeatures),
		local_mfa_enabled: S.optional(S.Boolean),
		ipv6_enabled_groups: S.optional(AccountSettingsInputIpv6EnabledGroupsList),
	}),
).annotate({ identifier: "AccountSettingsInput" }) as any as S.Schema<AccountSettingsInput>;

export interface AccountOnboarding {
	/** Indicates whether the account signup form is pending */
	signup_form_pending: boolean;
	/** Indicates whether the account onboarding flow is pending */
	onboarding_flow_pending: boolean;
}
export const AccountOnboarding = /*@__PURE__*/ S.suspend(() =>
	S.Struct({
		signup_form_pending: S.Boolean,
		onboarding_flow_pending: S.Boolean,
	}),
).annotate({ identifier: "AccountOnboarding" }) as any as S.Schema<AccountOnboarding>;

export interface AccountsAccountIdPutRequest {
	/** The unique identifier of an account */
	accountId: string;
	settings: AccountSettingsInput;
	onboarding?: AccountOnboarding;
}
export const AccountsAccountIdPutRequest = /*@__PURE__*/ S.suspend(() =>
	S.Struct({
		accountId: S.String.pipe(T.Label()),
		settings: AccountSettingsInput,
		onboarding: S.optional(AccountOnboarding),
	}).pipe(T.Http({ method: "PUT", uri: "/api/accounts/{accountId}", code: 200 })),
).annotate({ identifier: "AccountsAccountIdPutRequest" }) as any as S.Schema<AccountsAccountIdPutRequest>;

/** List of groups to which users are allowed access */
export type AccountSettingsJwtAllowGroupsList = ReadonlyArray<string>;
export const AccountSettingsJwtAllowGroupsList = /*@__PURE__*/ S.Array(
	S.String,
) as any as S.Schema<AccountSettingsJwtAllowGroupsList>;

/** Limits which peer groups are allowed to expose services. If empty, all peers are allowed when peer expose is enabled. */
export type AccountSettingsPeerExposeGroupsList = ReadonlyArray<string>;
export const AccountSettingsPeerExposeGroupsList = /*@__PURE__*/ S.Array(
	S.String,
) as any as S.Schema<AccountSettingsPeerExposeGroupsList>;

/** List of group IDs whose peers receive IPv6 overlay addresses. Peers not in any of these groups will not be allocated an IPv6 address. New accounts default to the All group. */
export type AccountSettingsIpv6EnabledGroupsList = ReadonlyArray<string>;
export const AccountSettingsIpv6EnabledGroupsList = /*@__PURE__*/ S.Array(
	S.String,
) as any as S.Schema<AccountSettingsIpv6EnabledGroupsList>;

export interface AccountSettings {
	/** Enables or disables peer login expiration globally. After peer's login has expired the user has to log in (authenticate). Applies only to peers that were added by a user (interactive SSO login). */
	peer_login_expiration_enabled: boolean;
	/** Period of time after which peer login expires (seconds). */
	peer_login_expiration: number;
	/** Enables or disables peer inactivity expiration globally. After peer's session has expired the user has to log in (authenticate). Applies only to peers that were added by a user (interactive SSO login). */
	peer_inactivity_expiration_enabled: boolean;
	/** Period of time of inactivity after which peer session expires (seconds). */
	peer_inactivity_expiration: number;
	/** Allows blocking regular users from viewing parts of the system. */
	regular_users_view_blocked: boolean;
	/** Allows propagate the new user auto groups to peers that belongs to the user */
	groups_propagation_enabled?: boolean;
	/** Allows extract groups from JWT claim and add it to account groups. */
	jwt_groups_enabled?: boolean;
	/** Name of the claim from which we extract groups names to add it to account groups. */
	jwt_groups_claim_name?: string;
	/** List of groups to which users are allowed access */
	jwt_allow_groups?: AccountSettingsJwtAllowGroupsList;
	/** Enables or disables DNS resolution on the routing peers */
	routing_peer_dns_resolution_enabled?: boolean;
	/** Allows to define a custom dns domain for the account */
	dns_domain?: string;
	/** Allows to define a custom network range for the account in CIDR format */
	network_range?: string;
	/** Allows to define a custom IPv6 network range for the account in CIDR format. */
	network_range_v6?: string;
	/** Enables or disables peer expose. If enabled, peers can expose local services through the reverse proxy using the CLI. */
	peer_expose_enabled: boolean;
	/** Limits which peer groups are allowed to expose services. If empty, all peers are allowed when peer expose is enabled. */
	peer_expose_groups: AccountSettingsPeerExposeGroupsList;
	extra?: AccountExtraSettings;
	/** Enables or disables experimental lazy connection */
	lazy_connection_enabled?: boolean;
	/** Set Clients auto-update version. "latest", "disabled", or a specific version (e.g "0.50.1") */
	auto_update_version?: string;
	/** When true, updates are installed automatically in the background. When false, updates require user interaction from the UI. */
	auto_update_always?: boolean;
	/** Enables or disables client metrics push for all peers in the account */
	metrics_push_enabled?: boolean;
	/** Limits the dashboard to the Agent Network surface for this account. Set for accounts created via netbird.ai signups and can be disabled later. Enabling this requires dashboard_features.agent_network to be true in the same request. */
	agent_network_only?: boolean;
	dashboard_features?: AccountDashboardFeatures;
	/** Indicates whether the embedded identity provider (Dex) is enabled for this account. This is a read-only field. */
	embedded_idp_enabled?: boolean;
	/** Indicates whether local (email/password) authentication is disabled. When true, users can only authenticate via external identity providers. This is a read-only field. */
	local_auth_disabled?: boolean;
	/** Enables or disables TOTP multi-factor authentication for local users. Only applicable when the embedded identity provider is enabled. */
	local_mfa_enabled?: boolean;
	/** List of group IDs whose peers receive IPv6 overlay addresses. Peers not in any of these groups will not be allocated an IPv6 address. New accounts default to the All group. */
	ipv6_enabled_groups?: AccountSettingsIpv6EnabledGroupsList;
}
export const AccountSettings = /*@__PURE__*/ S.suspend(() =>
	S.Struct({
		peer_login_expiration_enabled: S.Boolean,
		peer_login_expiration: S.Number,
		peer_inactivity_expiration_enabled: S.Boolean,
		peer_inactivity_expiration: S.Number,
		regular_users_view_blocked: S.Boolean,
		groups_propagation_enabled: S.optional(S.Boolean),
		jwt_groups_enabled: S.optional(S.Boolean),
		jwt_groups_claim_name: S.optional(S.String),
		jwt_allow_groups: S.optional(AccountSettingsJwtAllowGroupsList),
		routing_peer_dns_resolution_enabled: S.optional(S.Boolean),
		dns_domain: S.optional(S.String),
		network_range: S.optional(S.String),
		network_range_v6: S.optional(S.String),
		peer_expose_enabled: S.Boolean,
		peer_expose_groups: AccountSettingsPeerExposeGroupsList,
		extra: S.optional(AccountExtraSettings),
		lazy_connection_enabled: S.optional(S.Boolean),
		auto_update_version: S.optional(S.String),
		auto_update_always: S.optional(S.Boolean),
		metrics_push_enabled: S.optional(S.Boolean),
		agent_network_only: S.optional(S.Boolean),
		dashboard_features: S.optional(AccountDashboardFeatures),
		embedded_idp_enabled: S.optional(S.Boolean),
		local_auth_disabled: S.optional(S.Boolean),
		local_mfa_enabled: S.optional(S.Boolean),
		ipv6_enabled_groups: S.optional(AccountSettingsIpv6EnabledGroupsList),
	}),
).annotate({ identifier: "AccountSettings" }) as any as S.Schema<AccountSettings>;

export interface Account {
	/** Account ID */
	id: string;
	settings: AccountSettings;
	/** Account domain */
	domain: string;
	/** Account domain category */
	domain_category: string;
	/** Account creation date (UTC) */
	created_at: string;
	/** Account creator */
	created_by: string;
	onboarding: AccountOnboarding;
}
export const Account = /*@__PURE__*/ S.suspend(() =>
	S.Struct({
		id: S.String,
		settings: AccountSettings,
		domain: S.String,
		domain_category: S.String,
		created_at: S.String,
		created_by: S.String,
		onboarding: AccountOnboarding,
	}),
).annotate({ identifier: "Account" }) as any as S.Schema<Account>;

export interface AccountsGetRequest {}
export const AccountsGetRequest = /*@__PURE__*/ S.suspend(() =>
	S.Struct({}).pipe(T.Http({ method: "GET", uri: "/api/accounts", code: 200 })),
).annotate({ identifier: "AccountsGetRequest" }) as any as S.Schema<AccountsGetRequest>;

export type AccountsGetResponseBodyList = ReadonlyArray<Account>;
export const AccountsGetResponseBodyList = /*@__PURE__*/ S.Array(
	Account,
) as any as S.Schema<AccountsGetResponseBodyList>;

export type AccountsGetResponse = AccountsGetResponseBodyList;
export const AccountsGetResponse = /*@__PURE__*/ S.suspend(() =>
	AccountsGetResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({ identifier: "AccountsGetResponse" }) as any as S.Schema<AccountsGetResponse>;

export type AccountsAccountIdDeleteError = BadRequest | Forbidden | NetbirdOpError;
/** Delete an Account Deletes an account and all its resources. Only account owners can delete accounts. */
export const accountsAccountIdDelete: API.OperationMethod<
	AccountsAccountIdDeleteRequest,
	AccountsAccountIdDeleteResponse,
	AccountsAccountIdDeleteError,
	NetbirdOpContext
> = /*@__PURE__*/ API.make(() => ({
	input: AccountsAccountIdDeleteRequest,
	output: AccountsAccountIdDeleteResponse,
	errors: [BadRequest, Forbidden, UnknownNetbirdError],
	protocol: NetbirdProtocol,
	retry: Retry.Retry,
}));

export type AccountsAccountIdPutError = BadRequest | Forbidden | NetbirdOpError;
/** Update an Account Update information about an account */
export const accountsAccountIdPut: API.OperationMethod<
	AccountsAccountIdPutRequest,
	Account,
	AccountsAccountIdPutError,
	NetbirdOpContext
> = /*@__PURE__*/ API.make(() => ({
	input: AccountsAccountIdPutRequest,
	output: Account,
	errors: [BadRequest, Forbidden, UnknownNetbirdError],
	protocol: NetbirdProtocol,
	retry: Retry.Retry,
}));

export type AccountsGetError = BadRequest | Forbidden | NetbirdOpError;
/** List all Accounts Returns a list of accounts of a user. Always returns a list of one account. */
export const accountsGet: API.OperationMethod<
	AccountsGetRequest,
	AccountsGetResponse,
	AccountsGetError,
	NetbirdOpContext
> = /*@__PURE__*/ API.make(() => ({
	input: AccountsGetRequest,
	output: AccountsGetResponse,
	errors: [BadRequest, Forbidden, UnknownNetbirdError],
	protocol: NetbirdProtocol,
	retry: Retry.Retry,
}));
